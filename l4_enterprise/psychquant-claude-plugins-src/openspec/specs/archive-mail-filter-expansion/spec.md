# archive-mail-filter-expansion Specification

## Purpose

Defines how `/archive-mail` expands beyond sender-based search to capture complete thread corpora using subject keywords, participant aliases, and thread-subject expansion.

## Requirements

- Subject-keyword search expands email corpus
- Thread-subject expansion discovers sibling messages
- Participant aliases config field
