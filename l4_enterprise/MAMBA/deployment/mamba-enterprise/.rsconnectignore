# Ignore large data files - app uses Supabase in production
data/backups/
data/etl/
data/deprecated/
*.duckdb
*.wal

# DM_R054 v2.1 §6 override: meta_data.duckdb is the canonical runtime metadata
# source (no CSV fallback), so it MUST ship with the app bundle. The leading
# '!' negates the *.duckdb rule above for this one file.
!data/local_data/meta_data.duckdb

# Development files
.git/
.claude/
.Rproj.user/
.RData
.Rhistory
renv/

# Build artifacts
_targets/
logs/
*.log
