# 必須遵守的規則

**使用時機**: 每次對話都適用

---

## MP029 - 禁止 Fake Information（絕對禁止）

**核心原則**：Never assume; always verify before acting（不假設，先驗證）

### 1. DATA - 禁止假資料
- NO sample data, placeholder values, dummy data
- 如需資料但不可用：停止操作，詢問用戶
- 使用真實資料來源或生產資料的子集

### 2. FUNCTIONS - 先搜尋再建立 (DEV_R012)
- 建立新函數前，先搜尋 global_scripts/ 是否已存在
- 不要假設「函數不存在」

### 3. FILES - 先追蹤再修改 (DEV_R039)
- 修改檔案前，先 grep 確認哪個檔案「實際被使用」
- 不要假設「檔案名 = 實際使用的檔案」
- 範例：`grep 'source.*module_login' app_*.R` 確認實際載入的模組

### 4. PATHS - 先驗證再使用
- 確認路徑存在且正確
- 追蹤依賴鏈

**ENFORCEMENT**: 違反 MP029 視為嚴重錯誤。

> **Sister to MP163** (Progressive Completeness, 2026-04-30):MP163 sentinels(`'unclassified'` / `'UNKNOWN'`)是**explicit gap markers**,不是捏造資料 — 兩個原則同方向,MP163 是 MP029 處理「人工輸入未填」的正確做法(visibly 標出來而非靜默 drop 或捏造)。

---

## 原則查找流程

### 主要方法：llm/ 資料夾（v5.0）

1. **必讀** `00_principles/llm/index.yaml`
   - 主要入口（核心原則 + scenarios + chapter index）
   - 結構化規則（CNL + Datalog-like 雙格式）
   - 明確的 `dependencies` 區塊

2. **找到你的 scenario** 在 `scenarios:` 區塊
   - 每個 scenario 列出 `must_read` 原則
   - 按需檢查 `chapter_files`
   - 遵循該 scenario 的 `checklist`

3. **按需讀取 chapter files**:
   - `llm/CH00_meta.yaml` - 124 Meta-Principles
   - `llm/CH01_structure.yaml` - Structure & Organization
   - `llm/CH02_data.yaml` - Data Management
   - `llm/CH03_development.yaml` - Development Methodology
   - `llm/CH04_ui.yaml` - UI & UX
   - `llm/CH05_testing.yaml` - Testing & QA
   - `llm/CH06_ic.yaml` - Integration & Collaboration (Autopush)
   - `llm/CH07_ux.yaml` - User Experience
   - `llm/CH08_data_presentation.yaml` - Data Presentation & Visualization
   - `llm/CH19_documentation.yaml` - Documentation (Wiki, Changelog, Reporting)
   - `llm/CH09_database.yaml` - Database (DuckDB) Specs
   - `llm/CH11_etl.yaml` - ETL Pipeline Patterns
   - `llm/CH12_derivations.yaml` - Derivations
   - `llm/CH13_modules.yaml` - Modules & Tools
   - `llm/CH14_functions.yaml` - Functions Reference
   - `llm/CH15_apis.yaml` - APIs & External Integration
   - `llm/CH16_connections.yaml` - Connections
   - `llm/CH17_templates.yaml` - Templates & Examples
   - `llm/CH18_solutions.yaml` - Solutions & Patterns

4. **如需更多細節**，讀 .qmd 檔案：
   - 位置: `docs/en/part1_principles/{CHAPTER}/{ID}_{name}.qmd`

---

## 原則階層（Decision Priority）

衝突時優先順序：
1. **Meta-Principles (MP)** - 系統架構基礎
2. **Principles (P)** - 實作指引
3. **Rules (R)** - 具體實作模式
4. `global_scripts/` 既有程式碼模式
5. 業界最佳實踐（不與原則衝突時）

---

## 關鍵原則快速參考

### 架構基礎
- **MP122**: Penta-Track Subrepo Architecture (v4.0, 2026-04-12) - 五條 git submodule tracks:global_scripts、update_scripts、nsql、config、**handbook**(新增,工讀生 operational guidance)
- **MP165** (v1.3, 2026-05-14, #668): Dashboard Presence as Pipeline Acceptance — **DRV-Layer Step 2 Propagation (v1.3 amendment)**:Step 2「有意義的值」(Meaningful Values)從 v1.1 dashboard 層 enforcement 傳遞到 DRV 層。新 gate 腳本 `shared/global_scripts/23_deployment/drv_output_shape_gate.R` 獨立於 `dashboard_presence_gate.R`(input source 差異:DBI 直查 DuckDB 表 vs headless browser 對 Shiny UI)但共用 `contracts.R` primitives 可適用部分。**3-level 顆粒度**:**L1 表存在**(必要,`DBI::dbExistsTable`)+ **L2 列數下限**(必要,`COUNT(*) >= l2_row_count_min`)+ **L3 predictor-type 分布**(選用,適用 Poisson-analysis 類表,`GROUP BY predictor_type` 比對 declared `required` types)。Per-company 契約住 `shared/global_scripts/98_test/e2e/contracts/<company>_drv.yaml`(與 dashboard `<company>.yaml` 並列,**不**在 `app_config.yaml`)。**Pipeline 整合**:`update_scripts/_targets.R` 加 `verify_drv_output_shape` final target 依賴所有 D{NN} derivation 目標,`make run` 在 strict-mode 下 critical fail = exit 非零。**MP163 sentinel 邊界**:sentinel-covered rows(`'unclassified'` / `'UNKNOWN'` / 領域 marker)預設**計入** L2 和 L3(對齊 MP163 Progressive Completeness 的「永遠可跑」);選用 `sentinel_ratio_max` per-contract opt-in 警告當 sentinel 比例 > 閾值。**30 天 warn-mode 過渡**:預設 warn-mode 至 2026-06-13(30 天 post-merge),sunset 後翻 strict-mode,missing yaml 硬性失敗;per-company `strict_mode: true` opt-in pre-sunset 可用;對齊 MP102 v1.3 reviewed_by legacy-sunset 模式。**Defense in depth**:dashboard 層 Step 2 (v1.1) 抓 consumer-side regression,DRV 層 Step 2 (v1.3) 讓 producer-side vacuous-pass 不可能。**為何 amendment 非姊妹 MP166**:v1.1 已把 Step 2 framed 為跨層 quality 軸,dashboard 跟 DRV 是同原則的兩個 enforcement 點;sibling MP166 會有同樣 Step 2 詞彙 + 高 cross-ref density(強訊號 = 一個原則)。觸發:#668 — 2026-05-13 QEF/D_RACING/MAMBA 跨公司 adaptive walk surface 系統性 vacuous-pass(QEF amz 189 列只 time_feature / D_RACING amz 表缺 / MAMBA cbz 同 pattern),儀表板層 Step 2 抓得到但只在 user 走訪後;DRV 層 enforcement 在 producer 端 prevent。 — **Dual-track architecture (v1.2 amendment)**:**Track A** = declarative baseline(hand-maintained per-company yaml + `dashboard_presence_gate.R`,closed-world,deterministic — 儀表板測試的 floor)+ **Track B** = adaptive overlay(`/shiny-adaptive-walk` skill in `r-shiny-debugger` plugin v1.1+,self-converging loop via safari-browser + LLM judge,real_bug → `/idd-issue` / test_infra_gap → 自動 mutate yaml,per-iter safety gate + auto-rollback,CONVERGED/PLATEAUED/DIMINISHING verdict — 儀表板測試的 ceiling)。**(C) Aggressive authority** for Track B skill:可改 mutable zone(`98_test/e2e/**`、`dashboard_presence_gate.R`、`fn_debug_mode.R`),不可改 immutable zone(`10_rshinyapp_components/**`、`16_derivations/**`、`fn_analysis_*.R`、`update_scripts/ETL/**`、`update_scripts/DRV/**`、principle `.qmd`)— 改 immutable 處的 defect 改走 `/idd-issue`。Declarative 是 floor,adaptive 是 ceiling,兩軸都成立才完整 MP165 conformance。`MP165_ADAPTIVE_BUDGET=100` env var 限制 LLM judge call/session。Plugin 耦合:`r-shiny-debugger` plugin 版本必須 ≥ 1.1。 — **Two-axis acceptance framework (v1.1 amendment)**:**Quality axis** = **Step 1: No Red Errors**(0 個 R `stop()`/uncaught error / 0 個 `[DEBUG_MODE_NULL_OUTPUT]` / 0 個 UI 紅字 / 0 個 JS console error)+ **Step 2: Meaningful Values**(KPI > threshold / DataTable rows ≥ min 排除 placeholder / Plotly traces 非空 / dropdowns ≥ min_choices / 無 `-`/NULL/NA where 應有真值)。**Context axis** = **Local check**(dev-time iterate-friendly:critical fail-loud,warning 容許;`Rscript app.R` + agent-browser + console grep;每次 commit 前 + 切 branch 後)+ **Deploy check**(pre-deploy gate strict:任何 critical fail 拒絕 deploy push;`dashboard_presence_gate.R`)。**Tier ↔ Step mapping**:Tier 1 lite cover Step 1 only(NULL marker scan);Tier 2 full cover Step 1+2(full contract suite);Tier 3 CI cover Step 1+2(per-module subset)。**Tier 是 timing 軸,Step 是 quality 軸,兩者不一對一**。Step 1 不是單一 Tier 任務;`no_red_error_text` assertion 跨 Tier 2/3 也跑;`[DEBUG_MODE_NULL_OUTPUT]` 是 Tier 1 fast signal,deploy-gate 再驗一次。2×2 框架住 principle 層,Tier 系統住 implementation 層。Step 1 是 Step 2 的必要但不充分:零紅字 + KPI 全 `-` = Step 1 PASS / Step 2 FAIL。 — **v1.0 原始內容(下方)**: Dashboard Presence as Pipeline Acceptance — Pipeline acceptance contract **從資料庫寫入完成延伸到使用者可見的渲染層**。`make run` 完成所有 derivation target **不算成功**,除非每個 dashboard tab,在每個 active platform / product_line 下,渲染滿足宣告的 per-element contracts(KPI 值閾值 / DataTable 排除 placeholder 後有意義列數 / Plotly trace 數 / filter dropdown 選項數)。**三層 enforcement**:**(1) Tier 1 DRV-end lite**(`update_scripts/DRV/_targets.R` 加 final target `dashboard_smoke_lite`,啟 headless Shiny + `SHINY_DEBUG_MODE=TRUE` 偵測 `[DEBUG_MODE_NULL_OUTPUT]`,~5s/run,任何 NULL → fail)/ **(2) Tier 2 deploy-gate full**(`23_deployment/dashboard_presence_gate.R` 在 `make deploy-push` 前跑,evaluate per-company contract YAML 的每個 contract,critical fail = refuse deploy,~60s)/ **(3) Tier 3 CI selective**(per-PR 讀 `git diff` 透過 `affected_modules.R` 推導模組,只跑 affected,~15s)。**Contract primitive library** `98_test/e2e/contracts/contracts.R` 提供 5 個 per-element 原語:`assert_kpi_card_value` / `assert_datatable_meaningful_rows`(預設 `exclude_placeholders = TRUE`,關鍵強化:legacy `assert_datatable_has_rows(min_rows = 1)` 在 QEF hsg 1 列 `Ideal` placeholder 案例錯誤通過)/ `assert_plotly_has_data_points` / `assert_filter_dropdown_choices` / `assert_chart_axis_non_empty`。**Per-company contract YAML**(`98_test/e2e/contracts/<company_lowercase>.yaml`)宣告 `modules.<name>.enabled` + `sub_tabs.<id>.{active_product_lines, contracts}`;contracts 含 `selector` / `assertion` / `severity: critical|warning`。**Hard fail discipline**:**禁止** global `soft_warn_mode` toggle;唯一 opt-out 路徑為 (a) DRV target 不存在(declarative-dependency 自然 opt-out,KITCHENMAMA 沒 BrandEdge 即此種)或 (b) per-contract `severity: warning`(Tier 2 回報但不阻擋 deploy;Tier 1 不看 severity)。對齊 user「打通 + 不會紅字」訴求 — 早期 fail-loud at DRV gate 防止使用者畫面晚期紅字。**Sister 原則**:MP162 per-bridge ship-readiness 軸(L4 至少 1 module render > 0 rows)↔ MP165 per-dashboard-tab × per-platform × per-product_line 軸(complementary unit pair)/ MP163 input-completeness 軸(sentinel discipline)↔ MP165 rendering-acceptance 軸(orthogonal pair,皆需滿足才達使用者可見完整)/ MP102 v1.3 上游 bridge yaml authoring contract ↔ MP165 下游 rendering acceptance contract / MP029 verify-before-acting general ↔ MP165 verify-rendering-presence specific。**Backbone(2026-05-10 amendment per #604/#605)**:Tier 1 lite + Tier 2 deploy gate 用 **`nohup Rscript app.R + agent-browser`**(非 shinytest2)。理由:shinytest2 stability detector 在 plotly heavy render aborts 且 chromote subprocess 寫到 temp dir 不寫 `.shiny-debug/shiny.log`(DEV_R054 path)。`shinytest2` 保留給 unit-style 單 output assertion;dashboard 層級 e2e 用 Rscript+agent-browser。`test-brandedge.R` 暴露 `MP165_USE_AGENT_BROWSER=true` env var 委派到 agent-browser path。IC_P002 cross-co verified PASS:QEF 119 contracts / D_RACING 13 / MAMBA 10。**Files Bound**:`98_test/e2e/contracts/contracts.R` / `98_test/e2e/contracts/*.yaml` / `98_test/e2e/test-*.R` / `98_test/e2e/run_smoke_lite.R` / `23_deployment/dashboard_presence_gate.R` / `update_scripts/DRV/_targets.R` / `04_utils/fn_debug_mode.R`(reuse) + Layer 2/3 sync。**Enforcement**:PR review level — 引入 `soft_warn_mode` 或弱化 `assert_datatable_meaningful_rows` 為 `assert_datatable_has_rows(min_rows = 1)` 不排除 placeholder = MP165 violation;process level — 每個 dashboard tab 在 contract YAML 必須有至少一個 per-element-type contract;架構 level — bound files 改動觸發 IC_P002 cross-co verify(QEF/D_RACING/MAMBA/WISER/kitchenMAMA)。觸發:#599 — 2026-05-10 `/idd-implement #595` close session,user 觀察「我現在其實還先不用部署就看到 QEF 沒正常顯示了」直接打臉 acceptance ladder 7/7 宣稱;3 個 root causes(hardcoded 英文 empty-state / AI scoring 資料缺口 50/54 NA / `translate()` 未 vectorize 紅字)在 #595 Phase A/B/C verification chain 全漏掉。MP165 codify rendering-acceptance 軸,讓未來 verification(human or AI)不再 regress 到 acceptance ladder over-claim。
- **MP163** (2026-04-30): Progressive Completeness — Pipeline 與 derivation **永遠可跑到底**,不論人工輸入完整度為何。**四道 gate**:**(1) Always-Runnable Pipeline**:ETL/DRV scripts SHALL NOT 在缺人工輸入時 `stop()` / `abort()`;hard failure 只保留給 engineering failures(DB 連線斷、schema 損壞、code defect),不給 absent business inputs(unmapped ASIN / 缺 brand classification / Gsheet 還沒填的 row)。**(2) Visible-Gap Sentinel Materialization**:human-input gaps materialize 為**可見 sentinel**(`'unclassified'` / `'UNKNOWN'` / domain-specific marker),**不**靜默 drop、**不**用 `NULL`/`NA` 在 user-facing tables、**不**藏進別的 bucket aggregate。是 MP154 field-level rule 的 architectural 升級。**(3) Surface-to-Attention Gaps**:gap 必須 surface 到 business-accessible attention loop — coverage audit table(canonical 命名 `df_*_coverage_audit`)/ derived Gsheet / dashboard panel / email digest;**禁止**只埋進 log file 或 runtime warning。**(4) Asymptotic Completeness Without Redeploy**:人工 maintainer 補一筆缺漏 → 下次 ETL run 自動 cover、unclassified count 自然下降;**零 code change、零 schema migration、零 redeploy**。**Sister principles**:MP154 field-level sentinel(escalation pair)/ MP155 minimize human input(corollary pair — MP163 是 MP155 不完整時的 pipeline behaviour)/ MP162 time-axis ship-readiness(orthogonal pair — MP163 是 human-input-completeness axis)/ MP029 sentinel ≠ fake information(compatible — sentinel 是 explicit gap marker,不是捏造)。**Files Bound**:`shared/update_scripts/ETL/**/*.R` / `shared/global_scripts/16_derivations/fn_D*_core.R` / `shared/global_scripts/05_etl_utils/**/*.R` + canonical 命名 `df_*_coverage_audit` for surface tables。**Anti-patterns**:`stop()` on missing input / `NULL` instead of sentinel / log-only gap surface / 100% completeness gating。**Compliant patterns**:LEFT JOIN + COALESCE to sentinel / coverage audit table 自動 populate / derived Gsheet 連到 dashboard。**Enforcement**:PR review level — 引入 `stop()` on missing human input 是 MP163 violation,除非 commit message 明文說明 engineering-correctness 必要;架構 level — bound files 改動觸發 IC_P002 cross-co verify。觸發:#505 — 2026-04-30 `/spectra-discuss` 處理 #504/#412/#461 axis 2 design 時 user 觀察到 QEF 19,079 unclassified rows 不是 bug 而是 architectural commitment 在運作(88 self ASIN 缺 mapping → sentinel bucket → derived Gsheet 給業務 → 補完下次 ETL 自動 cover)。本原則把這個 implicit commitment 升為顯性 Tier-1 principle。
- **MP162** (2026-04-29): Bridge End-to-End Realization — Bridge ship-readiness 分 5 級 staged but L5 mandatory:**L1**(yaml lint clean + reviewed_by 結構化 + sibling review log CONVERGED/DIMINISHING)/ **L2**(`fn_glue_bridge` 跑完,target raw table rowcount > 0)/ **L3**(downstream DRV 跑完,output 表非空;reference bridge 無 DRV 則 vacuously satisfied)/ **L4**(至少一個 Shiny module 渲染出 > 0 rows,沒有 empty-state fallback 觸發)/ **L5**(end-to-end 行數對帳:rows_at_source = rows_at_raw + intentional_drops = rows_at_app_data + intentional_drops,所有 drop 都可解釋)。**Staged but mandatory**:可 ship 在 L1 短期 iterate L2-L5,但**不可**永遠停在 L<5(預設 30 天 envelope L1→L5,production ceiling 90 天,逾期需開 `bridge-rung:Lk-stale` issue)。**Ratchet property**:bridge 達到 Lk 後不可 regress(會 regress 的修改是 CRITICAL 違例,除非開 `bridge-rung:Lk-regression` issue 註明理由)。**Sister 原則 MP160**:MP160 governs authoring-time exhaustiveness(write side,bridge yaml 寫完整),MP162 governs consumption-time exhaustiveness(run side,bridge 真的跑到底)— 兩者都需要,write 完整無 run 完整 = shelf-ware,run 完整無 write 完整 = 無追蹤的部分覆蓋。**例外**:`bridges/{COMPANY}/wip/` 子目錄 / 無 DRV 的 reference bridges(L3 vacuously satisfied)/ 透過 `bridge-rung:Lk-stale` 顯式 deferral 的 bridge。**Enforcement**:`/glue-bridge` SKILL.md 在每次 invocation 後 surface 當前 Lk + 提議 next-level 工作;agent **不可**在 L<5 標記 bridge "done" 除非有顯式 deferral issue。觸發:#503 — 2026-04-28 blb iter 2 ensemble 證實 #502 spectra change 修復後 bridge yaml 正確(max severity CRITICAL → MEDIUM),user 觀察「bridge yaml correct」是手段不是目的,真正目標是資料完整到儀表板無遺漏;`/glue-bridge` 目前止於 L1(yaml lint),L2-L5 ad-hoc 容易漏。 ← **Sister to MP163** (Progressive Completeness, 2026-04-30):MP162 governs time/ship-readiness 軸(eventual L5 mandatory),MP163 governs human-input-completeness 軸(always runnable at any L);兩軸 orthogonal,需各自滿足。
- **MP161** (2026-04-28): Company-Scoped Schema Recognition — Schema yamls 和 generated DDL artifacts **必須**分類為 UNIVERSAL(所有 consuming 公司共用)或 COMPANY-SCOPED(僅一家公司資料)。**Universal 路徑**:`_authoring/{core_schemas.yaml,platform_extensions/}`(authoring) + `_generated/{core,platforms}/`(DDL 輸出)。**Company-scoped 路徑**:`_authoring/companies/{COMPANY}/...`(authoring) + `_generated/companies/{COMPANY}/platforms/{platform}/...`(DDL 輸出)。**決策準則**:「如果另一家公司明天加入,他們會不會用一模一樣的 shape?」YES → universal;NO → company-scoped。**例**:`sales` / `customers` / `company_product_extension`(generic 財務概念)= universal;`product_attributes_blb..sss` × 12(QEF eyewear-specific)= company-scoped → 應該放 `_authoring/companies/QEF_DESIGN/`。**邊界 case**:promotion(company-scoped → universal,當 2nd 公司採用)+ demotion(universal → company-scoped,當發現只有 1 家用)都需 spectra change + IC_P002 verify。**Enforcement**:`_build.R` 依 source schema 路徑 emit 到 scope-correct DDL 路徑;`validate_bridge_yaml.R` schema-aware check 在 `companies/{bridge.company}/` 也會找 datatype。**例外**:`company_product_extension` 雖然目前只有 QEF 用但 fields 是 generic accounting 概念(SKU/marketplace/cost/fee/profit)→ 留在 core_schemas.yaml(universal),Chinese source col 名稱(商品成本-廠價 等)只活在 QEF bridge 的 column_mapping。觸發:qef-product-master-redesign Phase 9 review (#499)
- **MP160** (2026-04-28): AI Schema/Bridge Authoring Completeness — 當 AI 撰寫具有可枚舉元素集合的 structured artifact(schema yaml `required_fields`/`optional_fields`、bridge yaml `column_mapping`/`ignored_columns`/`value_map`、`platform_extensions/*.yaml` 的 datatype 宣告),AI **必須** 在 authoring time 完整列出整個集合。**禁止**的 partial-completion patterns:(1) `optional_fields:` 留空或標 `# TODO`;(2) N-of-M 完成(寫一個 PL 留 11 個 `# TODO replicate`);(3) `column_mapping` 只覆蓋 required 欄位、`ignored_columns:` 留空;(4) `ignored_columns:` 短於未 map 的 source 集合。**機械等式檢查**:`set(source_columns) === set(column_mapping.keys) ∪ set(ignored_columns)`。**理由**:AI 沒有人類認知疲勞,marginal cost 寫完是分鐘級;部分完成的 artifact 結構合法但語意不全,後續 bridge author 會誤判 Layer 1 該被修改(實際是 Layer 1 sender 偷懶)。**例外**:runtime-unknown types(仍須列入,用 `notes:` 解釋)/ `bridges/{COMPANY}/wip/` 子目錄(非 runtime consumed)。Enforcement:`validate_bridge_yaml.R` 的 `check_mp160_exhaustive_enumeration()` 報 CRITICAL + `glue-bridge` SKILL.md Step 6 mandate + `spectra-propose` Step 6 No-Placeholders。觸發:qef-product-master-redesign Phase 9 task 9.2 review (#497)
- **MP159** (2026-04-28): Glue Authoring Boundary Immutability — bridge yaml authoring session **不可**修改 Layer 1 canonical schema 檔(`core_schemas.yaml` + `platform_extensions/*.yaml`)。source 對映困難時用 bridge 內既有 primitives:`ignored_columns`(source 多餘欄位)/`apply_fallback`(source 缺 canonical 必要欄位)/`value_map`(per-language 值翻譯)/`pre_filter`(per-source 資料品質)/`from_columns + merge_strategy`(source 多欄合併,DM_R065)。決策準則:「這個事實是 per-source 還是 universal?」per-source → bridge yaml;universal → 中止 bridge 寫作,另開 Layer 1 change spec。**例外**:dedicated Layer 1 refactor task(如 9.1 thin-master refactor)是合法的,差別在 task intent。Enforcement:`/glue-bridge` skill Step 0 preflight + `validate_bridge_yaml.R` 驗證每個 column_mapping 引用都存在於當前 schema(否則 CRITICAL)。觸發:qef-product-master-redesign Phase 9 review (#494)
- **MP158** (2026-04-28): Glue-Driven Raw Layer Extensibility — 新 canonical raw table 用**3-step 組合**新增,**不寫 per-table 客製 R ETL**:Step 1 enrich `core_schemas.yaml`/`platform_extensions/`(MP157 Layer 1)→ Step 2 跑 `_build.R` 產 DDL → Step 3 寫 `bridges/{COMPANY}/{platform}/{source}.bridge.yaml`(MP157 Layer 2)。**三軸成長**:新 datatype(全 3 步)/ 新 platform(全 3 步)/ **新公司(只需 Step 3)— canonical schema 已 universal,onboarding 第 6/7/N 家成本 bounded**。決策準則:「這個工作我願意為第 6/7/12 家公司重複嗎?」願意 → 走 MP158;不願意 → 在寫該成 bridge 的客製 code。Anti-pattern:`*_ETL_*_0IM.R` 客製 / per-co schema fork / 偽造 fingerprint。觸發:#489 ship 後 + qef-product-master-redesign Phase 9 spectra-ask 釐清
- **MP157** (2026-04-28): Two-Layer YAML Authoring — Schema authoring 必分兩層:Layer 1 = canonical schema yaml(universal contract,DDL codegen 來源,`_authoring/core_schemas.yaml` + `platform_extensions/*.yaml`)+ Layer 2 = bridge yaml(per-co/per-source translation,`_authoring/bridges/{COMPANY}/{platform}/{source}.bridge.yaml`)。**Layer 2 不參與 DDL codegen** — `_build.R` 只讀 Layer 1。決策準則:「這個事實如果換家公司會變嗎?」會變 → Layer 2;不變 → Layer 1。Anti-pattern:per-co conditional 進 Layer 1 / bridge 重定義 canonical type。觸發:#489 + qef-product-master-redesign Phase 9 ingest
- **MP156**: Two-Tier Normativity - Tier-1 conceptual rules (`00_principles/*.qmd`) 與 Tier-2 implementation specs (yaml/csv/SQL configs in code chapters) 都 normative;principles SHALL bind tier-2 files via `binds:` frontmatter + Files Bound + Change Discipline;三類(Schema Contracts/Path Registries/Authoring Dictionaries)有不同 binding-discipline 強度
- **MP155**: Minimize Human Input - 資料欄位先回答 4 問判定 input/view/action surface,預設系統 derive,人類補卡住的;view-only pull-based,Action 是 Input 延伸 ← **Sister to MP163** (Progressive Completeness):MP163 定義人類還沒補完時 pipeline 該怎麼 behave,是 MP155 的 corollary。
- **MP154**: Side Effect Defense - 操作不可靜默改變資料語意；mapping 失敗用 sentinel（如 `"UNKNOWN"`），不可用 NA ← **Sister to MP163** (Progressive Completeness):MP163 是 MP154 field-level sentinel rule 的 architectural escalation;系統範圍 MP154 + 加上 surface + asymptotic = MP163。
- **MP064**: ETL-Derivation Separation - ETL 處理資料準備，Derivations 處理商業邏輯
- **MP092**: Platform ID Standard
- **MP093**: Script Separation Principle
- **MP103**: autodeinit() Behavior - 完整清理移除所有變數
- **MP104**: ETL Data Flow Separation
- **MP102** (v1.3, 2026-04-29): ETL Output Standardization — 第一個 MP156 canonical example;v1.2 binds 6 個 Tier-2 schema 檔(`01_db/raw_schema/_authoring/` 下的 core_schemas / schema_registry / transformed_schemas / platform_extensions / r_definitions / IMPLEMENTATION_GUIDE),Category A Schema Contracts。**v1.3 amendment(per #500)**:bridge yaml 的 `reviewed_by` 從**字串**升級為**結構化 yaml mapping**(human_reviewer / review_mode / ai_reviewers / findings_summary / approved_at / review_artifacts);findings_summary.critical AND high SHALL 為 0 才 ship-ready;sibling `<source>.bridge.review.md` 必須存在且含 `## Verdict: CONVERGED` 或 `## Verdict: DIMINISHING`;當 findings_summary.medium + low > 0 時必須有 `findings_resolutions:` block。**Self-converging loop**:`/glue-bridge` skill Step 6a-6e,4 reviewers(codex + correctness + security + devils-advocate),三個收斂條件(CONVERGED / DIMINISHING / PLATEAUED)+ anti-oscillation invariant,沒有 max-iteration cap。Legacy 字串 `reviewed_by: "che"` 接受到 2026-07-31(deprecation WARNING),之後 reject。修改 bound files 必跑 DOC_R009 三層同步 + IC_P002 cross-company verify(QEF/D_RACING/MAMBA/WISER/kitchenMAMA)+ commit message `Verified:` trailer。觸發:#489(v1.2)+ #500(v1.3 ad-hoc ensemble review session 找出 v1.2 的 3 structural gaps)

### 資料管理
- **DM_R028**: ETL Data Type Separation - 腳本遵循 `{platform}_ETL_{datatype}_{phase}.R`(**v2.4 amendment, 2026-05-14, #670**:scope clarification — 此 pattern 僅適用 ETL 層;DRV 層由 DM_R066 規範)
- **DM_R066** (2026-05-14, #670): DRV Platform Agnosticism — DRV scripts(`update_scripts/DRV/**/*.R`)**SHALL NOT** 在 filenames / 目錄路徑 / header tags / SQL templates / output table literals 等任何結構性表面嵌入平台識別字(`cbz`/`amz`/`eby`/`shp`/`tiktok`)。平台識別 SHALL 於 runtime 由三層 fallback 解析:**(1)** `Sys.getenv("DRV_PLATFORM")` 主要(orchestrator path)→ **(2)** `Sys.getenv("MAMBA_PLATFORM")` fallback(既有 orchestrator env var,向後相容)→ **(3)** `--platform <code>` CLI flag last(直接 Rscript 呼叫)。三者全空 SHALL `stop()` fail-fast,錯誤訊息結構化 + 列出所有 source。Invalid 平台(不在 `get_platform_config()` active list)亦 fail-fast。解析機制 SHALL 用單一 helper(`resolve_drv_platform()`)— 不在 SQL templates 散落 `Sys.getenv()`。**Scope**:universal,所有 DRV groups(D01-D11)+ 5 家公司(QEF_DESIGN/D_RACING/MAMBA/WISER/kitchenMAMA)。**Filesystem 結構**:DRV scripts 住在 group-based 子目錄(`DRV/D04/`、`DRV/all/`),**不**住 per-platform subdir(`DRV/cbz/`)。Orchestrator(`_targets.R`)`resolve_script_path()` 優先 group-based 路徑、fallback 至 per-platform 路徑(transition 期內向後相容)。**Sibling to DM_R028**:ETL 因來源 schema 真的不同(CBZ JSON vs Amazon ASIN)per-platform parser 合理;DRV 在 canonical schema 之上(`canonical → canonical`)algorithm 與平台無關,per-platform file = 純粹命名重複,無 algorithm 上的正當理由。`<platform>_<group>_<seq>.R` 寫法 = anti-pattern;`PLATFORMS <- c("cbz", "eby")` 寫法(`update_all_platforms_poisson.R` 內)= meta-coupling anti-pattern。**Migration**:漸進 — 一次重構一個 DRV group,先 D04(Poisson)當 canonical demo,後續 D01/D03/D05 separate spectra changes。**Files Bound**:`shared/update_scripts/DRV/**/*.R` + `shared/update_scripts/_targets.R` + `shared/global_scripts/04_utils/fn_resolve_drv_platform.R` + DM_R028 .qmd(amended)。**Enforcement**:PR review level — 違反此規則 = MERGE 拒絕;process level — `<plat>_<group>_<seq>.R` 寫法觸發 spectra change requirement;架構 level — bound files 改動觸 IC_P002 cross-co verify。觸發:#670 — 2026-05-14 #668 close-out 對話發現 #661 root cause 第三層 anti-pattern。
- **DM_R023**: Universal DBI Approach (R092) - 使用 `dbConnect_universal()`
- **DM_R025**: Type Conversion Between R and DuckDB
- **DM_R036**: ETL Return Value Patterns

### 開發
- **DEV_R057** (2026-05-02): Session-Start Toolchain Readiness Verification — R session 在 `UPDATE_MODE` / `GLOBAL_MODE` 啟動時 SHALL verify(1) R 版本 ≥ 4.4.0、(2) canonical `core` package list 全部裝好、(3) `optional` 缺則 warn 不擋。**APP_MODE skip**(對齊 DM_R063 deploy bundle purity)。**AI 撞缺套件 SHALL `install.packages(missing)` 不 workaround** — 改用 `dbReadTable + R-side filter` 取代 `tbl2 + dplyr` 是禁止的,違反 DM_R023 universal data access。Canonical core / optional 清單 single source of truth 在 `04_utils/fn_check_session_packages.R`;CLI wrapper(`98_test/check_packages.R`)+ autoinit hook 都 source 同一檔。**第一動作**(任何 R 工作之前):`Rscript shared/global_scripts/98_test/check_packages.R` — 缺則 install,然後再做 R 工作。Files Bound:`fn_check_session_packages.R`(helper)+ `98_test/check_packages.R`(CLI)+ `sc_Rprofile.R` autoinit hook + `DEV_R057.qmd`(Layer 1)+ `llm/CH03_development.yaml`(Layer 2)+ `.claude/rules/11-session-readiness.md`(Layer 3 quick reference)。Anti-pattern:撞缺 dbplyr 改寫程式繞過(累積 non-canonical data access path,erosion DM_R023);Compliant:`install.packages('dbplyr')` 然後繼續 canonical 流程。Cross-co IC_P002 verify(QEF/D_RACING/MAMBA/WISER/kitchenMAMA)+ commit `Verified:` trailer for canonical list 修改。觸發:#507 — diagnose #475 fix 過程 AI 撞 `library(dbplyr)` missing 後選 workaround,user 觀察「理論上有指令裝套件不會撞」exposed Gap A(stale list)+ Gap B(無 rule)+ Gap C(ad-hoc /tmp/*.R bypass autoinit)。
- **DEV_R056** (2026-04-28): Project Primary Language — R First — 專案主要語言是 R,新程式碼一律 R 撰寫。**Python 唯一允許位置**:`shared/global_scripts/09_python_scripts/`(目前 4 個 NLP/AI 檔案)。**禁止**:其他位置寫 `.py` / Bash 寫 business logic / TypeScript 獨立工具。**三大反 pattern 拒絕**:(1)「Python 寫得快」→ 專案慣例蓋過個別腳本最佳化;(2)「我比較熟 Python」→ 不熟 R 就用 `r-docs-guide` skill;(3)「Python 有那個 library」→ 先驗證 R 是否有對等(CRAN >20000 packages)。**強度**:advisory / convention(不是 mechanical correctness — 違反 ≠ 程式錯,只是不 idiomatic)。Enforcement:PreToolUse hook `check-language-discipline.sh` 在 Write/Edit `.py` 檔(非 09_python_scripts/ 內)時 emit 警告 + 退出 0(不阻擋)。**邊界擴張**需走 spectra change + IC_P002 cross-co verify。觸發:qef-product-master-redesign Phase 9 task 9.2 (#498) — AI 提議寫 `.py` schema 生成器。
- **DEV_R001**: Apply Functions Over Loops
- **DEV_R014**: data.table Vectorization
- **SO_R007**: One Function One File

### UI 元件
- **UI_R001**: UI-Server-Defaults Triple Pattern
- **UI_R011**: bs4Dash Structure Adherence

### 資料呈現 (Data Presentation)
- **MP153**: Data Type Presentation Fidelity - 不同資料類型需用符合其語意的格式呈現
- **DP_R001**: Distribution Winsorization - 分佈圖極端值縮尾處理
- **DP_R002**: Integer Display Fidelity - 整數型資料不可顯示小數點

### 整合協作 / 版本控制（Autopush + Issue 管理）
- **IC_P001**: Universal Immediate Sync - 每完成一個功能即 commit → pull → push
- **IC_P002**: Cross-Company Verification - 修改 shared/ 下 subrepo 檔案前,必須驗證對所有消費公司(QEF_DESIGN/D_RACING/MAMBA/WISER/kitchenMAMA)適用且無副作用;commit message 須含 `Verified:` trailer。觸發:#403/#457
- **IC_R001**: Global Scripts Synchronization - global_scripts 的嚴格版本
- **IC_R010** (2026-05-02): Issue Source Confidence Triage — 每個 issue **必須**標恰好一個 `confidence:*` label(`confidence:confirmed` / `confidence:vague` / `confidence:exploratory`),跟 type / priority / company / module / source 自由疊加(orthogonal)。**3D taxonomy**:Type × Confidence × Schedule(milestone presence)。**5 destinations 路由**:confirmed → source-doc milestone + Company project;vague → source-doc milestone + `needs:scope-clarification` + Company project;exploratory + considering → Lab project(`Considering` column)ONLY no milestone;exploratory + committed-future → `Future Planning` milestone(repo-wide singleton no due date)+ Lab project(`Future Planning` column)。**Lab project** standard 4 columns:`Considering` / `Future Planning` / `In Progress` / `Done`(對應 schedule 軸)。**Plugin step**:`idd-issue` Step 4.4 + `idd-diagnose` Step 3.4 — predicate-driven default-on(document source + 2+ issues),advisory + AskUserQuestion always,**禁止** silent classify。Heuristic = keyword regex(scholar 全名 / 完整 prompt template / algorithm 細節 → exploratory;截圖 + 客戶反映語氣 → confirmed),user override always wins。**State machine**:exploratory + Lab only ──[team commits]──→ + Future Planning;vague ──[clarify]──→ confirmed;confirmed ──→ close。**Files Bound**:`IC_R010.qmd`(Layer 1)+ `llm/CH06_ic.yaml`(Layer 2)+ `.claude/rules/12-issue-routing.md`(Layer 3)+ GitHub artifacts(`confidence:*` labels / `Future Planning` milestone / `Marketing Theory Lab` project)。Anti-patterns:exploratory issue 進 source-doc milestone(routing violation)/ silent classify without AskUserQuestion / Future Planning 設 due date / confidence 取代 type label(orthogonality violation)。Rollback escape hatch:`SKIP_CONFIDENCE_TRIAGE=true` env var。Cross-co IC_P002 verify 5 公司 + commit `Verified:` trailer for canonical list 修改。觸發:#509 — `向創建議_20260411` milestone 混雜客戶反映 + 老師學術理論 expansion(#412/#401 客戶反映 vs #406/#407/#409/#410/#411/#419 Keller/Holt/Kapferer/Sinek/Ansoff theory),engineer 浪費 design effort + 客戶 progress 不可分辨。
- **IC_R011** (2026-05-03): Commercial Project Low-Bar Issue Filing — 在 commercial-project codebase 中(default-on for `kiki830621/ai_martech_*` family + 5 公司 ai_martech_l4_enterprise consumers),AI agent **應**(SHOULD)傾向 file GitHub issue 對任何 verifiable concern — bug / design ambiguity / observed friction / deferred work / sister bug / out-of-scope item with verifiable form — 而非 default 跳過「太小不值得開」。**When in doubt: file the issue**。Cost ratio 30:1 偏向 filing(30 秒 file vs 30 分鐘 reconstruct)。**Default-on triggers**:verifiable behavior gap(即使 1 行 fix)/ skill plugin design ambiguity / code review minor smell / Plan tier verifiable out-of-scope / closing comment "follow-up"/"deferred" / sister bug 從 reproduction 撞到 / out-of-scope items user 可能忘的。**Default-off exemptions(narrow)**:純探索(走 IC_R010 confidence:exploratory routing not 跳開)/ 既有 issue 已 cover / AI hallucinated 但無 codebase evidence(per MP029 verify first)/ CONSTRAINT not TODO(deliberate non-action)/ meta self-reference。**Scope(Decision 2 = C)**:default-on for `ai_martech_*` / `PsychQuant/*` 商用 / 任何 CLAUDE.md 標 commercial 的 repo;預設 off for personal / OSS / hobby。**Strictness(Decision 1 = B)**:SHOULD 而非 SHALL,permits judgment within explicit triggers + exemptions。**Rollback hatch(Decision 3 = C, defense in depth)**:env var `AI_LOW_BAR_ISSUE_FILING=false`(臨時)+ repo CLAUDE.md `# Disable IC_R011`(永久)。**Application(Decision 4 = B)**:AI primary,human encouraged 但 not enforced。**跟 IC_R010 disambiguation**:IC_R010 routes opened issues; IC_R011 governs whether to open。orthogonal,both apply。**跟 MP155 orthogonal**:MP155 minimize user INPUT;IC_R011 maximize audit TRAIL — 不同軸,both reduce maintenance debt。**Files Bound**:`IC_R011.qmd`(Layer 1)+ `llm/CH06_ic.yaml`(Layer 2)+ `.claude/rules/01-always.md`(Layer 3,本檔)+ AI agent behavioral norm(no specific code files bound — bind 是 heuristic governing 開 issue decision)。Anti-patterns:silent skip / discuss-only never file / closing summary 提 follow-up but no issue / CONSTRAINT vs DEFERRED conflation / sister bug 只在 conversation 提沒 file。Compliant 範例(本 session):#455 close → 4 follow-ups(#511 / #512 / #513 / #514);#510 manual reproduction → sister #518 + #520;#510 verify → #519;#516 Plan-tier out-of-scope → #521 / #522 / #523;skill design gap → #515;user instruction → #516(本 principle 自身)。共 11 follow-up issues filed in session,each ~30 sec,audit trail intact。Rollback escape hatch:`AI_LOW_BAR_ISSUE_FILING=false` env var。Cross-co IC_P002 verify 5 公司 + commit `Verified:` trailer。觸發:#516 — User explicit instruction in #455 close conversation 「商業專案不管多小的問題都要盡量開 issue」;AI default heuristic 在 session 中反覆 conservative bias,user 反覆 explicit-override → 證明 AI default 對 commercial context 校準錯誤。

---

## 檔案位置標準

**原則檔案僅在 docs/ 目錄：**
- `docs/en/part1_principles/` - English version (.qmd format)
- `docs/zh/part1_principles/` - Chinese version (.qmd format)

**根目錄 .md 檔案不是官方原則。**
